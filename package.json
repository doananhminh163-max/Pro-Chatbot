{
  "name": "report-analyzing-root",
  "private": true,
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:broker": "npm --prefix backend run dev:broker",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:opencode": "npm --prefix backend run dev:opencode",
    "dev": "concurrently -n opencode,backend,frontend -c yellow,cyan,green \"npm run dev:opencode\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "kill": "npm --prefix backend run kill"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
